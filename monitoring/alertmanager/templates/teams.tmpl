{{ define "teams.critical" }}
{
  "@type": "MessageCard",
  "@context": "https://schema.org/extensions",
  "summary": "🚨 Critical Alert: {{ .GroupLabels.alertname }}",
  "themeColor": "d63333",
  "sections": [
    {
      "activityTitle": "🚨 CRITICAL ALERT",
      "activitySubtitle": "jira2solidtime monitoring",
      "facts": [
        {
          "name": "Alert",
          "value": "{{ .GroupLabels.alertname }}"
        },
        {{ range .Alerts }}
        {
          "name": "Summary",
          "value": "{{ .Annotations.summary }}"
        },
        {
          "name": "Description",
          "value": "{{ .Annotations.description }}"
        },
        {
          "name": "Status",
          "value": "{{ .Status }}"
        },
        {
          "name": "Started",
          "value": "{{ .StartsAt.Format "2006-01-02 15:04:05" }}"
        }
        {{ end }}
      ]
    }
  ],
  "potentialAction": [
    {
      "@type": "OpenUri",
      "name": "📊 View Dashboard",
      "targets": [
        {
          "os": "default",
          "uri": "http://localhost:3000/d/jira2solidtime"
        }
      ]
    },
    {
      "@type": "OpenUri",
      "name": "🔧 Prometheus",
      "targets": [
        {
          "os": "default",
          "uri": "http://localhost:9090"
        }
      ]
    }
  ]
}
{{ end }}

{{ define "teams.warning" }}
{
  "@type": "MessageCard",
  "@context": "https://schema.org/extensions",
  "summary": "⚠️ Warning: {{ .GroupLabels.alertname }}",
  "themeColor": "ff8c00",
  "sections": [
    {
      "activityTitle": "⚠️ WARNING",
      "activitySubtitle": "jira2solidtime monitoring",
      "facts": [
        {
          "name": "Alert",
          "value": "{{ .GroupLabels.alertname }}"
        },
        {{ range .Alerts }}
        {
          "name": "Summary",
          "value": "{{ .Annotations.summary }}"
        },
        {
          "name": "Description",
          "value": "{{ .Annotations.description }}"
        },
        {
          "name": "Status",
          "value": "{{ .Status }}"
        },
        {
          "name": "Started",
          "value": "{{ .StartsAt.Format "2006-01-02 15:04:05" }}"
        }
        {{ end }}
      ]
    }
  ],
  "potentialAction": [
    {
      "@type": "OpenUri",
      "name": "📊 View Dashboard",
      "targets": [
        {
          "os": "default",
          "uri": "http://localhost:3000/d/jira2solidtime"
        }
      ]
    }
  ]
}
{{ end }}